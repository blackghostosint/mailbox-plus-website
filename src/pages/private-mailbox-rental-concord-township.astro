---
import Layout from '../layouts/Layout.astro';
import { pageMeta } from '../config/pageMeta';
import PrivateMailboxRentalPage from '../page-components/private-mailbox-rental-concord-township';
import { WithProviders } from '../page-components/WithProviders';

const meta = pageMeta['/private-mailbox-rental-concord-township'] ?? { title: 'Mailbox Plus Ohio', description: 'Community-focused pack & ship retail store in Concord Township, Ohio.' };
---

<Layout title={meta.title} description={meta.description} schema={meta.schema}>
    <WithProviders component={PrivateMailboxRentalPage} client:only="react" />
</Layout>
