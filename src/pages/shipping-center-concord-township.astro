---
import Layout from '../layouts/Layout.astro';
import { pageMeta } from '../config/pageMeta';
import ShippingCenterPage from '../page-components/shipping-center-concord-township';
import { WithProviders } from '../page-components/WithProviders';

const meta = pageMeta['/shipping-center-concord-township'] ?? { title: 'Mailbox Plus Ohio', description: 'Community-focused pack & ship retail store in Concord Township, Ohio.' };
---

<Layout title={meta.title} description={meta.description} schema={meta.schema}>
    <WithProviders component={ShippingCenterPage} client:only="react" />
</Layout>
