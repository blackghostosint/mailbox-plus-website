---
import Layout from '../../layouts/Layout.astro';
import { pageMeta } from '../../config/pageMeta';
import { DigitalFingerprinting as DigitalFingerprintingPage } from '../../page-components/DigitalFingerprinting';
import { WithProviders } from '../../page-components/WithProviders';

const meta = pageMeta['/specialty/digital-fingerprinting'] ?? { title: 'Mailbox Plus Ohio', description: 'Community-focused pack & ship retail store in Concord Township, Ohio.' };
---

<Layout title={meta.title} description={meta.description} schema={meta.schema}>
    <WithProviders component={DigitalFingerprintingPage} client:only="react" />
</Layout>
