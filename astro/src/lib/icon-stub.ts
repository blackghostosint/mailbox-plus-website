import type { IconComponent } from '../types/services';

const StubIcon: IconComponent = () => null;

export default StubIcon;
export {
  StubIcon as AlertTriangle,
  StubIcon as Archive,
  StubIcon as Box,
  StubIcon as CheckCircle,
  StubIcon as Clock,
  StubIcon as FileText,
  StubIcon as Fingerprint,
  StubIcon as FolderOpen,
  StubIcon as Globe,
  StubIcon as HelpCircle,
  StubIcon as Layers,
  StubIcon as Lock,
  StubIcon as Mail,
  StubIcon as MapPin,
  StubIcon as NotebookText,
  StubIcon as Package,
  StubIcon as Palette,
  StubIcon as Printer,
  StubIcon as Scan,
  StubIcon as Scissors,
  StubIcon as Shield,
  StubIcon as Star,
  StubIcon as Truck,
  StubIcon as Users,
};
